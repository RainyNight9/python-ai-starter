# 🧪 测试流程指南

本文档将带你一步步测试项目的所有功能，确保每个模块都正常工作。

## 📋 测试前准备

### 1. 环境检查

```bash
# 检查 Python 版本（需要 3.8+）
python3 --version

# 检查依赖是否安装
pip list | grep fastapi
pip list | grep openai
```

### 2. 配置检查

确保 `.env` 文件已正确配置：

```bash
# 查看配置（不会显示敏感信息）
cat .env.example
```

必需配置项：
- `OPENAI_API_KEY` - 你的 API 密钥
- `OPENAI_BASE_URL` - API 网关地址
- `MODEL_NAME` - 模型名称
- `EMBEDDING_API_KEY` - 向量化 API 密钥（可选，不填则使用 OPENAI_API_KEY）

---

## 🚀 第一部分：后端功能测试

### 测试 1：启动后端服务

```bash
# 方式一：使用 Makefile
make backend

# 方式二：直接启动
python3 -m uvicorn app.main:app --reload
```

**预期结果：**
- 终端显示 `Application startup complete`
- 显示 `Uvicorn running on http://127.0.0.1:8000`
- 没有报错信息

**如果失败：**
- 检查端口 8000 是否被占用
- 检查 Python 依赖是否完整安装
- 查看错误信息，通常会提示缺少哪个包

---

### 测试 2：访问 API 文档

打开浏览器访问：`http://127.0.0.1:8000/docs`

**预期结果：**
- 看到 Swagger UI 界面
- 显示所有 API 接口列表
- 包含以下接口组：
  - Sessions（会话管理）
  - Chat（聊天接口）
  - Documents（文档管理）
  - History（历史记录）

**测试要点：**
- 界面加载正常
- 可以展开每个接口查看详情
- 可以看到请求参数和响应格式

---

### 测试 3：会话管理功能

#### 3.1 获取会话列表

在 API 文档中找到 `GET /api/sessions`，点击 "Try it out" → "Execute"

**预期结果：**
```json
[
  {
    "id": 1,
    "title": "默认会话",
    "created_at": "2024-01-01 12:00:00"
  }
]
```

**说明：**
- 首次启动会自动创建 ID 为 1 的默认会话
- 这是数据库迁移脚本自动完成的

#### 3.2 创建新会话

找到 `POST /api/sessions`，点击 "Try it out"

输入请求体：
```json
{
  "title": "测试会话"
}
```

点击 "Execute"

**预期结果：**
```json
{
  "id": 2,
  "title": "测试会话",
  "created_at": "2024-01-01 12:05:00"
}
```

**说明：**
- 新会话 ID 自动递增
- 创建时间为当前服务器时间

#### 3.3 删除会话

找到 `DELETE /api/sessions/{session_id}`

输入 `session_id`: `2`（刚创建的测试会话）

点击 "Execute"

**预期结果：**
```json
{
  "status": "success",
  "message": "Session 2 deleted"
}
```

**注意：**
- 不能删除最后一个会话（至少保留一个）
- 删除会话会同时删除该会话的所有聊天记录

---

### 测试 4：文档上传与 RAG 功能

#### 4.1 上传文档

找到 `POST /api/upload`，点击 "Try it out"

点击 "Choose File"，选择一个 PDF 或 TXT 文件（建议使用项目自带的 `uploads/程序员面试手册-电子书.pdf`）

点击 "Execute"

**预期结果：**
```json
{
  "status": "success",
  "message": "文件 程序员面试手册-电子书.pdf 处理成功，共切分出 XX 个知识块。"
}
```

**处理过程：**
1. 文件保存到 `uploads/` 目录
2. 文档被切分成多个小块（chunk）
3. 每个块通过 Embedding API 转换成向量
4. 向量存储到 `app/rag/vectorstore/` 目录

**如果失败：**
- 检查 `EMBEDDING_API_KEY` 是否配置正确
- 检查文件格式（只支持 PDF 和 TXT）
- 查看终端错误信息

#### 4.2 查看已上传文档

找到 `GET /api/documents`，点击 "Execute"

**预期结果：**
```json
[
  {
    "filename": "程序员面试手册-电子书.pdf",
    "size_bytes": 1234567,
    "modified_at": 1704096000
  }
]
```

#### 4.3 查看 RAG 配置

找到 `GET /api/rag/settings`，点击 "Execute"

**预期结果：**
```json
{
  "embedding_model": "text-embedding-v3",
  "top_k": 3,
  "chunk_size": 500,
  "chunk_overlap": 50
}
```

**配置说明：**
- `top_k`: 检索时返回最相关的 3 个文档片段
- `chunk_size`: 每个文档块 500 字符
- `chunk_overlap`: 相邻块重叠 50 字符

---

### 测试 5：流式聊天（带 RAG）

找到 `POST /api/chat/stream`，点击 "Try it out"

输入请求体：
```json
{
  "message": "程序员面试需要准备什么？",
  "system_prompt": "你是一个专业的面试辅导老师",
  "session_id": 1
}
```

点击 "Execute"

**预期结果：**
- 首行返回 `::META::` 开头的 JSON，包含 RAG 引用信息
- 后续返回模型的流式文本输出
- 引用信息包含：文件名、页码、相似度距离、片段预览

**示例输出：**
```
::META::{"rag_citations":[{"source":"程序员面试手册-电子书.pdf","page":5,"distance":0.234,"snippet_preview":"面试准备包括..."}]}
根据参考资料，程序员面试需要准备以下几个方面...
```

**测试要点：**
- 回答内容与上传的文档相关
- 引用信息准确显示文档来源
- 流式输出逐字显示

---

### 测试 6：Agent 工具调用

找到 `POST /api/chat/agent`，点击 "Try it out"

#### 6.1 测试计算器工具

输入请求体：
```json
{
  "message": "帮我计算 (123 + 456) * 789 / 12",
  "system_prompt": "",
  "session_id": 1
}
```

**预期结果：**
```json
{
  "reply": "计算结果是 38072.25",
  "steps": [
    {
      "kind": "model_tool_calls",
      "round": 0,
      "detail": "模型请求 1 个工具"
    },
    {
      "kind": "tool_result",
      "round": 0,
      "tool_name": "calculate",
      "arguments": "{\"expression\":\"(123 + 456) * 789 / 12\"}",
      "result_preview": "38072.25"
    },
    {
      "kind": "model_final",
      "round": 1,
      "detail": "模型返回最终文本（无 tool_calls）"
    }
  ],
  "rag_citations": []
}
```

#### 6.2 测试时间查询工具

输入请求体：
```json
{
  "message": "现在几点了？",
  "system_prompt": "",
  "session_id": 1
}
```

**预期结果：**
- 返回服务器当前时间
- `steps` 显示调用了 `get_server_time` 工具

#### 6.3 测试文档检索工具

输入请求体：
```json
{
  "message": "从我上传的文档中查找关于算法的内容",
  "system_prompt": "",
  "session_id": 1
}
```

**预期结果：**
- Agent 自动调用 `search_uploaded_documents` 工具
- 返回文档中相关内容
- `rag_citations` 包含引用信息

#### 6.4 测试会话导出工具

输入请求体：
```json
{
  "message": "把当前会话导出为 Markdown 格式",
  "system_prompt": "",
  "session_id": 1
}
```

**预期结果：**
- Agent 调用 `export_session_markdown` 工具
- 返回 Markdown 格式的聊天记录

---

### 测试 7：历史记录管理

#### 7.1 查看历史记录

找到 `GET /api/history`，输入参数 `session_id`: `1`

**预期结果：**
```json
[
  {
    "role": "user",
    "content": "程序员面试需要准备什么？",
    "created_at": "2024-01-01 12:10:00"
  },
  {
    "role": "assistant",
    "content": "根据参考资料...",
    "created_at": "2024-01-01 12:10:05"
  }
]
```

#### 7.2 清空历史记录

找到 `DELETE /api/history`，输入参数 `session_id`: `1`

**预期结果：**
```json
{
  "status": "success",
  "message": "History cleared for session 1"
}
```

---

## 🎨 第二部分：前端功能测试

### 测试 8：启动前端服务

打开新终端窗口：

```bash
cd frontend
pnpm install  # 首次运行需要安装依赖
pnpm dev
```

**预期结果：**
- 显示 `Local: http://localhost:5173/`
- 浏览器自动打开或手动访问该地址

---

### 测试 9：前端界面功能

#### 9.1 界面布局检查

打开 `http://localhost:5173/`

**预期看到：**
- 左侧：会话列表，显示"默认会话"
- 中间：聊天区域，显示欢迎信息
- 右侧：知识库管理和 RAG 配置
- 顶部：会话标题和 Agent 模式开关
- 底部：系统提示词输入框和消息输入框

#### 9.2 会话管理测试

**创建新会话：**
1. 点击左侧"新建会话"按钮
2. 看到新会话出现在列表中
3. 自动切换到新会话

**切换会话：**
1. 点击不同的会话
2. 聊天区域内容随之切换
3. 当前会话高亮显示

**删除会话：**
1. 点击"删除会话"按钮
2. 当前会话被删除
3. 自动切换到剩余会话

#### 9.3 文档上传测试

1. 点击右侧"选择文件"按钮
2. 选择一个 PDF 或 TXT 文件
3. 点击"上传入库"按钮
4. 等待处理完成（状态栏显示进度）
5. 文档出现在文档列表中

**预期结果：**
- 状态栏显示"文件 XXX 处理成功，共切分出 XX 个知识块"
- 文档列表更新
- 可以点击"删除"按钮删除文档

#### 9.4 流式聊天测试

1. 在系统提示词框输入："你是一个 Python 专家"
2. 在消息输入框输入："Python 有哪些特点？"
3. 点击"发送"按钮

**预期结果：**
- 消息立即出现在聊天区
- AI 回复逐字显示（打字机效果）
- 如果有 RAG 引用，下方显示黄色引用卡片
- 引用卡片包含：文件名、页码、相似度、片段预览

#### 9.5 Agent 模式测试

1. 勾选顶部"Agent 模式"开关
2. 输入："帮我计算 100 * 200 + 300"
3. 点击发送

**预期结果：**
- AI 回复包含计算结果
- 下方显示"Agent 步骤"卡片
- 可以展开查看工具调用详情
- 显示调用了哪些工具、参数是什么、返回了什么

#### 9.6 RAG 配置查看

右侧"RAG 配置"区域显示：
- Embedding 模型名称
- Top K 值
- Chunk 大小和重叠

---

## 🔍 第三部分：集成测试场景

### 场景 1：完整的 RAG 问答流程

1. 上传一个技术文档（如 Python 教程）
2. 等待处理完成
3. 输入问题："这个文档讲了什么内容？"
4. 查看回答是否基于文档内容
5. 检查引用信息是否准确

**验收标准：**
- 回答内容与文档相关
- 引用显示正确的文件名和页码
- 片段预览与文档内容一致

---

### 场景 2：多会话隔离测试

1. 创建会话 A，发送消息"我喜欢 Python"
2. 创建会话 B，发送消息"我喜欢 Java"
3. 切换回会话 A，查看历史记录
4. 切换到会话 B，查看历史记录

**验收标准：**
- 两个会话的历史记录完全独立
- 切换会话时内容正确显示
- 删除一个会话不影响另一个

---

### 场景 3：Agent 多工具协作

输入复杂问题：
```
现在几点了？帮我计算一下从现在到晚上 8 点还有多少分钟。
然后从我上传的文档中查找关于时间管理的内容。
```

**预期结果：**
- Agent 依次调用多个工具
- 先查询时间
- 再进行计算
- 最后检索文档
- 综合所有结果给出回答

**验收标准：**
- 步骤显示完整的工具调用链
- 每个工具的输入输出清晰
- 最终回答整合了所有信息

---

### 场景 4：向量库重建测试

1. 上传多个文档
2. 测试检索功能正常
3. 点击"重建"按钮
4. 等待重建完成
5. 再次测试检索功能

**验收标准：**
- 重建后检索功能正常
- 所有文档都能被检索到
- 引用信息准确

---

## 📊 测试结果记录

### 后端测试清单

- [ ] 服务启动成功
- [ ] API 文档可访问
- [ ] 会话列表获取
- [ ] 会话创建
- [ ] 会话删除
- [ ] 文档上传
- [ ] 文档列表查看
- [ ] RAG 配置查看
- [ ] 流式聊天（无 RAG）
- [ ] 流式聊天（带 RAG）
- [ ] Agent 计算器工具
- [ ] Agent 时间查询工具
- [ ] Agent 文档检索工具
- [ ] Agent 会话导出工具
- [ ] 历史记录查看
- [ ] 历史记录清空

### 前端测试清单

- [ ] 前端服务启动
- [ ] 界面布局正常
- [ ] 会话创建
- [ ] 会话切换
- [ ] 会话删除
- [ ] 文档上传
- [ ] 文档删除
- [ ] 流式聊天显示
- [ ] RAG 引用显示
- [ ] Agent 模式切换
- [ ] Agent 步骤显示
- [ ] 系统提示词生效
- [ ] 历史记录加载

### 集成测试清单

- [ ] 完整 RAG 问答流程
- [ ] 多会话隔离
- [ ] Agent 多工具协作
- [ ] 向量库重建

---

## 🐛 常见问题排查

### 问题 1：后端启动失败

**症状：** 运行 `make backend` 报错

**排查步骤：**
1. 检查 Python 版本：`python3 --version`
2. 检查依赖安装：`pip list`
3. 查看具体错误信息
4. 检查端口占用：`lsof -i :8000`

### 问题 2：文档上传失败

**症状：** 上传文档后报错

**排查步骤：**
1. 检查 `EMBEDDING_API_KEY` 配置
2. 检查文件格式（只支持 PDF/TXT）
3. 查看后端终端错误日志
4. 检查 `uploads/` 目录权限

### 问题 3：RAG 检索无结果

**症状：** 提问后没有引用信息

**排查步骤：**
1. 确认文档已成功上传
2. 检查 `app/rag/vectorstore/` 目录是否存在
3. 确认使用的是流式或 Agent 接口（非流式接口不支持 RAG）
4. 尝试重建向量库

### 问题 4：Agent 工具不调用

**症状：** Agent 模式下工具没有被调用

**排查步骤：**
1. 确认模型支持 function calling
2. 检查问题是否明确需要工具
3. 查看后端日志中的 Agent 步骤
4. 尝试更明确的指令

### 问题 5：前端无法连接后端

**症状：** 前端显示网络错误

**排查步骤：**
1. 确认后端服务正在运行
2. 检查 `vite.config.ts` 中的代理配置
3. 查看浏览器控制台错误信息
4. 确认端口 8000 和 5173 都可访问

---

## ✅ 测试完成标准

当以上所有测试项都通过时，说明项目功能完整可用：

1. ✅ 后端所有 API 接口正常响应
2. ✅ 前端界面功能完整
3. ✅ RAG 文档检索准确
4. ✅ Agent 工具调用正常
5. ✅ 多会话隔离有效
6. ✅ 流式输出流畅
7. ✅ 引用信息准确

---

## 📝 测试报告模板

测试完成后，可以填写以下报告：

```
测试日期：____年__月__日
测试人员：________
环境信息：
- Python 版本：______
- 操作系统：______
- 模型提供商：______

测试结果：
- 后端测试：通过 __/16 项
- 前端测试：通过 __/13 项
- 集成测试：通过 __/4 项

发现问题：
1. ________________
2. ________________

改进建议：
1. ________________
2. ________________
```

---

## 🎯 下一步

测试通过后，你可以：

1. 阅读 `LEARNING_GUIDE.md` 深入理解代码
2. 查看 `DEPLOYMENT.md` 了解部署方案
3. 尝试添加自己的工具函数
4. 优化前端界面样式
5. 接入更多模型提供商

祝测试顺利！🎉
