# 📖 Python AI 平台 - 源码学习指南

本文档是对本项目的深度拆解，旨在帮助你更好地理解代码逻辑。
我们在所有的 `.py` 和 `.html` 源码文件中都**添加了详细的中文注释**，建议对照着这份文档去阅读源码！

---

## 一、技术栈全景图

本项目采用了非常经典的轻量级现代化 Web 开发栈：

- **Web 框架：FastAPI** (高性能，天然支持 Async 异步，写 AI 项目首选)
- **数据库 ORM：SQLAlchemy** (让你可以用写 Python 对象的方式去操作数据库，不用写繁琐的 SQL 语句)
- **数据库：SQLite** (最轻量级的本地数据库，零配置直接运行，适合个人项目)
- **AI 交互：OpenAI SDK** (官方库，用于连接大模型。绝大多数国产模型也都兼容这套接口)
- **前端页面：原生 HTML/JS** (极简主义，去除 Vue/React 的门槛，专注于理解前后端交互本质)

---

## 二、项目核心流程解析

当我们**在前端页面输入一段话并点击发送**时，背后发生了什么？

### 步骤 1：前端发起请求 (`static/index.html`)
1. 用户点击发送，触发 JavaScript 中的 `sendMessage()` 函数。
2. JS 将用户输入的文本包装成 JSON 格式。
3. 通过 `fetch` API 向后端的 `/api/chat` 发起一个 `POST` 请求。

### 步骤 2：后端接收与数据验证 (`app/api/endpoints.py`)
1. FastAPI 监听到 `/chat` 的请求，进入 `chat_with_ai` 路由函数。
2. FastAPI 会自动使用 `Pydantic` 库中的 `ChatRequest` 模型对传过来的数据进行**校验**。如果格式不对（比如没有传 `message` 字段），它会自动拦截并返回 422 错误。

### 步骤 3：数据库操作 - 存入问题 (`app/api/endpoints.py`)
1. 路由函数的参数 `db: Session = Depends(get_db)` 是一种叫作**依赖注入**的高级技巧，它自动为我们提供了一个连通的数据库通道。
2. 我们实例化一个 `models.ChatMessage` 对象，并用 `db.add()` 和 `db.commit()` 把用户的这条消息存入 SQLite 数据库。

### 步骤 4：组装 AI 上下文记忆 (`app/api/endpoints.py`)
1. 数据库虽然存了历史记录，但大模型本身是没有记忆的。
2. 因此我们用 `db.query` 查出最近的 10 条聊天记录。
3. 我们将这些记录组装成 OpenAI 要求的字典列表格式（即：`[{"role": "user", "content": "..."}, {"role": "assistant", "content": "..."}]`）。

> **💡 行业延伸：大家都是怎么做上下文管理的？**
> 
> 目前我们在 [endpoints.py](python-ai-starter/app/api/endpoints.py) 中的做法（直接截断取最近 10 条，即 **Sliding Window 滑动窗口法**）是最基础的实现。但在真实的工业级产品（如 ChatGPT 官网、各类 AI 助手应用）中，为了平衡“上下文连贯性”和“Token 成本/长度限制”，通常会采用以下更高级的方案：
> 
> 1. **滑动窗口 + Token 计数 (Sliding Window with Token Count) - 业界标配**
>    - **做法**：不再固定截取“10条”，而是引入 `tiktoken` 库（OpenAI 官方分词器）来计算历史消息的 Token 数量。如果超过了模型的上下文限制（比如 4096 tokens），再从最老的消息开始丢弃。
>    - **优点**：充分利用模型的上下文窗口，不会因为用户某句话特别长而突然丢失记忆。
> 
> 2. **历史总结 (Conversation Summarization)**
>    - **做法**：当历史消息累计到一定长度时，后台偷偷调用一次较便宜的模型（比如 GPT-3.5），把之前的 20 条对话“总结”成一段几百字的摘要，并作为一条 `System Prompt` 放在最前面。
>    - **优点**：极大地节省了每次请求携带长上下文的 Token 成本，同时保留了久远的记忆。
> 
> 3. **向量数据库检索 (RAG 记忆)**
>    - **做法**：把用户的每一次对话都通过 Embedding 模型转成向量，存入向量数据库（如 Milvus, Chroma）。当用户提问时，先去数据库搜索最相关的几条历史记录，再拼接到上下文中。
>    - **场景**：适用于打造拥有“永久长期记忆”的数字分身或 AI 伴侣。
> 
> **最佳实践建议**：对于刚入门的项目，目前的固定条数截断已经足够跑通。如果想往高级进阶，**第一步建议升级为“滑动窗口 + Token 计数”策略**。

### 步骤 5：调用大模型 (`app/services/llm.py`)
1. 我们调用了 `generate_ai_response` 函数。
2. 这个函数使用了异步库 `AsyncOpenAI`。`await client.chat.completions.create(...)` 这一行非常关键：它把组装好的上下文记忆发给了远程的大模型服务器（比如 Qwen、ChatGPT）。
3. 由于使用了 `await` 异步等待，在这个空隙中，FastAPI 可以去处理其他人的请求，不会导致整个服务器卡死（这也是为什么 AI 平台必须用异步框架的原因）。
4. 拿到结果后，解析出文本内容并返回。

### 步骤 6：数据库操作 - 存入回答并返回 (`app/api/endpoints.py`)
1. 拿到 AI 的回答后，我们再次实例化一个 `models.ChatMessage(role="assistant", ...)` 将它存入数据库。
2. 最后，FastAPI 自动将返回值打包成 JSON（遵循 `ChatResponse` 的格式），顺着网线送回给前端。

### 步骤 7：前端渲染 (`static/index.html`)
1. 前端 `fetch` 拿到了后端的 JSON 回复。
2. 调用 `appendMessage` 函数，动态创建一个 `<div>` 标签，把 AI 的回复塞进去。
3. 界面更新，完成了一次完整的 AI 问答闭环！

---

## 三、推荐的源码阅读顺序

为了让你不被文件绕晕，强烈建议按照以下顺序打开文件阅读（**别忘了看文件里我给你写的注释**）：

1. 👉 **`app/main.py`**：这是整个后端的“大脑”，看看路由是怎么被挂载的，跨域是怎么解决的。
2. 👉 **`app/core/config.py`**：这是“神经枢纽”，看看环境变量（API Key, 模型名）是怎么被加载进来的。
3. 👉 **`app/db/database.py` & `models.py`**：这是“记忆系统”，看看怎么连上数据库的，以及表结构长什么样。
4. 👉 **`app/api/endpoints.py`**：这是“交通枢纽”，最核心的业务逻辑全在这里，重点看 `@router.post("/chat")`。
5. 👉 **`app/services/llm.py`**：这是“外联部”，看看是怎么和 OpenAI 官方或者阿里云 Qwen 打交道的。
6. 👉 **`static/index.html`**：这是“脸面”，看看最原生的 JavaScript 是怎么调用接口和渲染界面的。

> **祝你学习愉快！看不懂的地方随时把代码发给 AI 助手，让它为你讲解。**

---

## 四、开发工具推荐：查看 SQLite 数据库

本项目使用了 SQLite 本地数据库，数据都存在根目录的 `ai_platform.db` 文件中。由于这是一个二进制文件，你不能直接用文本编辑器打开它。

为了在 VS Code / Trae 中方便地查看和修改数据库里面的数据（比如看看用户的聊天记录有没有成功存入），推荐安装以下插件：

### 推荐插件：SQLite Viewer

这是一个非常轻量且好用的 SQLite 查看插件。

**如何安装和使用：**
1. 打开 IDE 左侧的 **扩展 (Extensions)** 面板（或者按快捷键 `Cmd+Shift+X`）。
2. 在搜索框中输入：`SQLite Viewer`（作者是 *Florian Klampfer*，图标通常是个羽毛笔或数据库小图标）。
3. 点击 **Install** 安装。
4. 安装完成后，在左侧的资源管理器（文件树）中找到 `ai_platform.db` 文件。
5. **单击该文件**，它不会再提示“无法打开”，而是会直接在编辑器中打开一个表格视图！
6. 你可以在里面看到 `chat_messages` 表，并且可以直接查看、过滤甚至修改里面的聊天记录数据。